/*
** EPITECH PROJECT, 2022
** intput
** File description:
** input
*/

#ifndef INPUT_HPP
#define INPUT_HPP

namespace arcade {

	enum Input {
		AKEY_NULL = 0,	// No input, or undefined key
		AKEY_A,			// `a` key
		AKEY_B,			// `b` key
		AKEY_C,			// `c` key
		AKEY_D,			// `d` key
		AKEY_E,			// `e` key
		AKEY_F,			// `f` key
		AKEY_G,			// `g` key
		AKEY_H,			// `h` key
		AKEY_I,			// `i` key
		AKEY_J,			// `j` key
		AKEY_K,			// `k` key
		AKEY_L,			// `l` key
		AKEY_M,			// `m` key
		AKEY_N,			// `n` key
		AKEY_O,			// `o` key
		AKEY_P,			// `p` key
		AKEY_Q,			// `q` key
		AKEY_R,			// `r` key
		AKEY_S,			// `s` key
		AKEY_T,			// `t` key
		AKEY_U,			// `u` key
		AKEY_V,			// `v` key
		AKEY_W,			// `w` key
		AKEY_X,			// `x` key
		AKEY_Y,			// `y` key
		AKEY_Z,			// `z` key
		AKEY_LBRACKET,	// `[` key
		AKEY_RBRACKET,	// `]` key
		AKEY_SEMICOLON, // `;` key
		AKEY_COMMA,		// `,` key
		AKEY_DOT,		// `.` key
		AKEY_QUOTE,		// `'` key
		AKEY_SLASH,		// `/` key
		AKEY_BACKSLASH, // '\' key
		AKEY_TILDE,		// `~` key
		AKEY_EQUAL,		// `=` key
		AKEY_MINUS,		// `-` key
		AKEY_PLUS,		// `+` key
		AKEY_MULTIPLY,	// `*` key
		AKEY_DIVIDE,	// `/` key
		AKEY_NUM0,		// `0` key
		AKEY_NUM1,		// `1` key
		AKEY_NUM2,		// `2` key
		AKEY_NUM3,		// `3` key
		AKEY_NUM4,		// `4` key
		AKEY_NUM5,		// `5` key
		AKEY_NUM6,		// `6` key
		AKEY_NUM7,		// `7` key
		AKEY_NUM8,		// `8` key
		AKEY_NUM9,		// `9` key
		AKEY_NUMPAD0,	// `0` key from pad
		AKEY_NUMPAD1,	// `1` key from pad
		AKEY_NUMPAD2,	// `2` key from pad
		AKEY_NUMPAD3,	// `3` key from pad
		AKEY_NUMPAD4,	// `4` key from pad
		AKEY_NUMPAD5,	// `5` key from pad
		AKEY_NUMPAD6,	// `6` key from pad
		AKEY_NUMPAD7,	// `7` key from pad
		AKEY_NUMPAD8,	// `8` key from pad
		AKEY_NUMPAD9,	// `9` key from pad
		AKEY_ESC,		// Escape key
		AKEY_LCTRL,		// Left Control key
		AKEY_LALT,		// Left Alt key
		AKEY_LSHIFT,	// Left Shift key
		AKEY_LSYSTEM,	// Left System key (Windows key)
		AKEY_RCTRL,		// Right Control key
		AKEY_RALT,		// Right Alt key
		AKEY_RSHIFT,	// Right Shift key
		AKEY_RSYSTEM,	// Right System key (Windows key)
		AKEY_ENTER,		// Enter key (return)
		AKEY_SPACE,		// Space key
		AKEY_BACKSPACE, // BackSpace key (delete)
		AKEY_MENU,		// Menu key
		AKEY_TAB,		// Tabulation key
		AKEY_PAGEUP,	// Page Up key
		AKEY_PAGEDOWN,	// Page Down key
		AKEY_START,		// Origin key
		AKEY_END,		// End key
		AKEY_INSERT,	// Insert key
		AKEY_DELETE,	// Delete key (suppr)
		AKEY_UP,		// Up arrow key
		AKEY_DOWN,		// Down arrow key
		AKEY_LEFT,		// Left arrow key
		AKEY_RIGHT,		// Right arrow key
		AKEY_F1,		// F1 key
		AKEY_F2,		// F2 key
		AKEY_F3,		// F3 key
		AKEY_F4,		// F4 key
		AKEY_F5,		// F5 key
		AKEY_F6,		// F6 key
		AKEY_F7,		// F7 key
		AKEY_F8,		// F8 key
		AKEY_F9,		// F9 key
		AKEY_F10,		// F10 key
		AKEY_F11,		// F11 key
		AKEY_F12,		// F12 key
		AKEY_F13,		// F13 key
		AKEY_F14,		// F14 key
		AKEY_F15,		// F15 key
		AEVENT_STOP,	// Stop event, that mean the game should be stopped
	};
}

#endif /* !INPUT_HPP */
